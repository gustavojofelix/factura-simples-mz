import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ispcfacil.app',
  appName: 'ISPC Fácil',
  webDir: 'dist/demo/browser'
};

export default config;
